# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ios-native-app-diagnostics' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ios-native-app-diagnostics
  pod 'AppCenter'

  target 'ios-native-app-diagnosticsTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ios-native-app-diagnosticsUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
